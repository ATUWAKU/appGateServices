pipeline {
    agent any
	triggers {
	  upstream 'appGateServices_CI'	
	}
    stages {
        stage('Ejecucion') {
            steps {                
				bat 'gradlew clean build test aggregate'
            }
        }
		stage{
			steps{
				emailext attachLog: true, body: 'Este es el resultado', compressLog: true, subject: 'Resultado RM appGateServices', to: 'david_muner@hotmail.com'
			}
		}
    }	
}
